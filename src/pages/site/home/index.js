import React, { Fragment, useState } from 'react';
import Header from '../../../components/header'

const Page = (props) => {

    return (
        <Fragment>

            <Header pages={'site'} />
            
            <h2>Sou a HOME</h2>

        </Fragment>
    );

}

export default Page;